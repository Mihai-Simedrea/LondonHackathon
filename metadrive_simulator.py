"""Compatibility wrapper for the canonical MetaDrive simulation module."""

from neurolabel.backends.metadrive.simulation import MAX_STEPS, RECORD_INTERVAL, simulate

__all__ = ["MAX_STEPS", "RECORD_INTERVAL", "simulate"]
